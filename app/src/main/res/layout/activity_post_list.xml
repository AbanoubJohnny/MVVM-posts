<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

	<data app:class="app.posts.johnny.abanoub.mvvm_posts.modules.posts.PostListActivity">
		<variable
			 name="viewModel"
			 type="app.posts.johnny.abanoub.mvvm_posts.modules.posts.PostListViewModel"/>
	</data>
	<RelativeLayout
		 android:layout_width="match_parent"
		 android:layout_height="match_parent"
		 android:gravity="center"
		 android:orientation="vertical">
		<ProgressBar
			 android:layout_width="wrap_content"
			 android:layout_height="wrap_content"
			 android:theme="@android:style/Widget.ProgressBar.Small"
			 app:mutableVisibility="@{viewModel.getLoadingVisibility()}"
			 android:layout_centerInParent="true"/>
		<android.support.v7.widget.RecyclerView
			 android:id="@+id/post_list"
			 android:layout_width="match_parent"
			 android:layout_height="match_parent"
			 app:adapter="@{viewModel.getPostListAdapter()}"
			 android:layout_centerInParent="true"/>
	</RelativeLayout>
</layout>